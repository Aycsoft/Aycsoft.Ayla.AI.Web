# 预构建宿主镜像：构建上下文需同时提供 dotnet publish 的 host/ 和 Vite 的 dist/。
FROM mcr.microsoft.com/dotnet/aspnet:10.0
USER $APP_UID
WORKDIR /app
COPY host/ .
COPY dist/ ./wwwroot/
ENV AiApiBaseUrl=http://crosscart-ai:8080
EXPOSE 8080
ENTRYPOINT ["dotnet", "CrossCart.AI.Web.Host.dll"]
